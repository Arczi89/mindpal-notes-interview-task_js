{
  "name": "mindpal-notes",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "npx eslint . --fix",
    "build-css": "sass styles/main.scss:styles/main.css --no-source-map",
    "watch-css": "sass --watch styles/main.scss:styles/main.css",
    "start": "npm run build-css && npx http-server -p 8000"
  },
  "keywords": [],
  "author": "Artur Szwagrzak",
  "license": "ISC",
  "devDependencies": {
    "@eslint/js": "^9.9.1",
    "eslint": "^9.9.1",
    "globals": "^15.9.0"
  }
}
